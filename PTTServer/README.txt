To build and run this application do the following steps:

1. Open the Terminal
2. Navigate to the root directory
    cd PTTServer/
3. Then run the following command to build and run the project using Maven
    ./mvnw spring-boot:run
